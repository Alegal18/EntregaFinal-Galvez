import styled from '@emotion/styled';
import { Link } from 'react-router-dom';

export const LogoCart = styled(Link)`
    display: flex;
    height: 30px;
    width: 30px;

    &:hover{
        cursor: pointer;
    }
    
`
export const Span = styled.span`
    font-size: large;
    color: #fff;
`
export const Div = styled.div`
    display: flex;
    align-items: center;
`